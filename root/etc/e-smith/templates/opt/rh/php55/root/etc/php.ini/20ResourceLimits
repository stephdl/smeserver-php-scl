max_execution_time                     = {
    my $et = $php55{MaxExecutionTime} || "30";
    $OUT .= "$et";
}
memory_limit                           = {
    my $ml = $php55{MemoryLimit} || "32M";
    $OUT .= "$ml";
}
