max_execution_time                     = {
    my $et = $php56{MaxExecutionTime} || "30";
    $OUT .= "$et";
}
memory_limit                           = {
    my $ml = $php56{MemoryLimit} || "128M";
    $OUT .= "$ml";
}
;added by stephdl see $php56{MaxInputTime}
max_input_time                     = {
    my $it = $php56{MaxInputTime} || "60";
    $OUT .= "$it";
}

